interface:
  display_name: "Review Queue Orchestrator"
  short_description: "Explicitly assume a project's PR review queue role"
  default_prompt: "Use $review-queue and assume the role of PR review queue orchestrator for this project."
policy:
  allow_implicit_invocation: false
