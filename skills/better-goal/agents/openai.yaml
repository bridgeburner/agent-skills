interface:
  display_name: "Better Goal"
  short_description: "Track durable work in ~/.sdd"
  default_prompt: "Use $better-goal to run this work through the ~/.sdd protocol with task tracking, evidence, and a completion audit."
