interface:
  display_name: "Better Goal"
  short_description: "Track restart-safe work in ~/.sdd"
  default_prompt: "Use $better-goal to create and execute restart-safe ~/.sdd tasks with explicit handoffs, evidence, and a completion audit."
